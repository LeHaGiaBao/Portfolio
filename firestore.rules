rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{store}/{memberId} {
      // Public access for development purposes.
      allow read, write;
    }
  }
}